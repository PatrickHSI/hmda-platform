pipeline {
  agent any

  tools {nodejs "NodeJS_9"}

  stages {
    stage('Test') {
      steps {
       nodejs(nodeJSInstallationName: 'nodejs') {
                      sh 'npm install'
                  }
         sh 'npm -v'
         sh 'npm i --prefix ./ newman'
      }
    }
  }
}