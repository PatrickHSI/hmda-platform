pipeline {
  agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
             script{
             echo $PATH

             cd /var/jenkins_home/tools/jenkins.plugins.nodejs.tools.NodeJSInstallation/node/bin
             ls -all
             ./node --version

             node --version
             npm --version
             }
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}