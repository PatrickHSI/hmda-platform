pipeline {
   agent any

  stages {
    stage('Test') {
     agent {
            docker {
              // Set both label and image
              label 'docker'
              image 'node:8-alpine'
              args '--name docker-node' // list any args
            }
          }
      steps {
           sh 'docker pull postman/newman'
      }
    }
  }
}