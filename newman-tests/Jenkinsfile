pipeline {
  agent any

  tools {nodejs "NodeJS_9"}

  stages {
    stage('Example') {
      steps {
       withEnv(["PATH+NODE=${tool name: 'NodeJS_9', type: 'jenkins.plugins.nodejs.tools.NodeJSInstallation'}/bin"]) {
         sh 'node -v'
       }
      }
    }
  }
}