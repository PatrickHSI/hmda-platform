pipeline {
  agent any

  tools {nodejs "NodeJS_9"}
  stages {
    stage('Test') {
      steps {
      // nodejs(nodeJSInstallationName: 'NodeJS_9') {sh 'npm install'}
       sh 'cd newman-tests && ls -l'
         sh '''
         cd newman-tests && tar -xf node-v10.16.0-linux-x64.tar.xz
         '''
         sh '''
         ls -l && 
         cd newman-tests && ./node-v10.16.0-linux-x64/bin/npm -v
         '''

      }
    }
  }
}